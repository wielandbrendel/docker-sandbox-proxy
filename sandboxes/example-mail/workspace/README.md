# Workspace

This is the shared workspace for example-mail. Files here are visible
to both the host and the agent inside the sandbox.

Add task instructions, prompts, or reference material here. The agent
reads `TOOLS.md` automatically.

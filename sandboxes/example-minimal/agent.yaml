name: example-minimal
description: "Minimal sandboxed OpenClaw agent — no integrations"

openclaw:
  version: "2026.5.7"
  port: 18789

workspaces:
  - .

env:
  TZ: "UTC"
  # Add your env vars here. Reference .env values with shell-style
  # interpolation, for example:
  #   OPENAI_API_KEY: $OPENAI_API_KEY

env_file: .env

gmail_filter:
  enabled: false

network:
  policy: deny
  allow:
    - "*.openai.com"
    - "*.chatgpt.com"
    - "chatgpt.com"
    - "nodejs.org"
    - "*.nodejs.org"
    - "*.npmjs.org"
    - "*.npmjs.com"

resources:
  cpus: "2"
  memory: "4g"

restart_policy: unless-stopped
